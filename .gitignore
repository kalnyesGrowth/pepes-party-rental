# QA + tooling artifacts. These are generated by /qa-site and Playwright runs and
# must never be committed or deployed: they ballooned the payload to 38MB and
# tanked the page-weight gate. Regenerate them locally whenever you need them.
qa-*.png
qa-*.jpeg
.playwright-mcp/
